# Super Resume Generator 🦸

A pet project to demonstrate AI integration into backend workflow. Uses Flask + traditional html, returns a docx file.

A humorous AI-powered resume generator built with Flask and Jinja2.

## Features
- Hyperbolic resume generation
- Competitiveness slider (0–10)
- DOCX resume output
- Structured AI → template pipeline

## Setup

```bash
git clone ...
cd resume-builder
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

Please use your own API KEY for local testing. You need to set the environment variable POE_API_KEY to access AI service on POE.
